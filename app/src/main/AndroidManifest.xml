<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bibliotecaraul">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BibliotecaRaul">
        <activity android:name=".tareas.DetallesLibrosActivity"></activity>
        <activity android:name=".tareas.MostrarLibrosActivity" />
        <activity android:name=".tareas.BorrarCategoriaActivity" />
        <activity android:name=".tareas.NuevaCategoriaActivity" />
        <activity android:name=".tareas.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>